const ImagePath = '../Assets/Images/';
const png = '.png';

const Images = {
  back: require(ImagePath + 'back' + png),
  background: require(ImagePath + 'background' + png),
  appIcon: require(ImagePath + 'appIcon' + png),
  onboarding: require(ImagePath + 'onboarding' + png),
  circle: require(ImagePath + 'circle' + png),
};

export default Images;
